[pytest]
# only look for tests under 02_tests/api
testpaths = 02_tests/api

# show extra summary info
addopts = -ra

# tell pytest to import your api/ and utils/ packages off the repo root
pythonpath = .

# set the env‐vars your config.py needs, every time
env =
    COGNITO_USER_POOL_ID=eu-central-1_AP5Xpw0cL
    COGNITO_APP_CLIENT_ID=abc123example
    LOCAL_JWKS_PATH=02_tests/api/data/mock_jwks.json
